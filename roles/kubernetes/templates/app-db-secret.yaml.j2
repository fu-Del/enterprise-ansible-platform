apiVersion: v1
kind: Secret
metadata:
  name: app-db-secret
  namespace: default
type: Opaque
stringData:
  DB_HOST: "{{ database_server }}"
  DB_PORT: "{{ database_port }}"
  DB_NAME: "{{ database_name }}"
  DB_USER: "{{ database_user }}"
  DB_PASSWORD: "{{ vault_database_app_password }}"
