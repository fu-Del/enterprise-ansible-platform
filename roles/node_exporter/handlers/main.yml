---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart node exporter
  ansible.builtin.systemd:
    name: node_exporter
    state: restarted
# handlers file for roles/node_exporter
