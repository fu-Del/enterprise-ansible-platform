# Kubernetes Control Plane backup
backup_k8s_control_plane_enabled: true

backup_server_host: "192.168.11.16"
backup_server_user: "backupsvc"
backup_server_base_dir: "/backup/data"

backup_client_node_name: "node03"

backup_work_dir: "/var/lib/backup-client"
backup_log_dir: "/var/log/backup-client"

backup_ssh_key_path: "/root/.ssh/backup_ed25519"

backup_retention_local_days: 2

k8s_backup_kubeconfig: "/etc/kubernetes/admin.conf"

k8s_backup_paths:
  - /etc/kubernetes
  - /etc/containerd
  - /etc/cni
  - /var/lib/kubelet/config.yaml
  - /var/lib/kubelet/kubeadm-flags.env
k8s_backup_cron_hour: "2"
k8s_backup_cron_minute: "45"
